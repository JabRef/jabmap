/**
 * Provides file access' functionality.
 */
class FileManager {
    constructor() { }

    /**
     * Reads a .jmp file with the saved mind map.
     * @param { string } path - The path of a map.
     */
    LoadMap(path) {

    }

    /**
     * Creates or rewrites a file with corresponding mind map.
     * @param { map } mindMap - The mind map to save into a file.
     */
    SaveMap(mindMap) {

    }
}